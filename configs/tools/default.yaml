replicate:
  dino_model: "adirik/grounding-dino:latest"         # Grounding DINO for text-to-box
  sam_model: "meta/sam-2:fe97b453a6455861e3bac769b441ca1f1086110da7466dbb65cf1eecfd60dc83"  # SAM 2 pinned version
  # Grounded-SAM-2 style knobs (passed through to the DINO wrapper if supported).
  # See reference implementation: https://github.com/IDEA-Research/Grounded-SAM-2/tree/main
  dino_params:
    box_threshold: 0.25
    text_threshold: 0.25
  inpaint_model: "black-forest-labs/flux-fill-pro:latest"  # FLUX Fill Pro inpainting
  inpaint_params: {}                                 # Optional extra args (e.g., steps/guidance/output_format)
  nano_banana_model: ""                              # Unused (LLM-only critique)
  mock: false

storage:
  download_timeout: 120

